"use client";

import { FormEvent } from "react";
import { HeritageMap } from "./HeritageMap";
import { MONUMENT_REGISTER_BASE_URL, statusLabel, typeBadge } from "@/lib/heritage-view-model";
import { useTermSuggestions } from "@/hooks/useTermSuggestions";
import { useBodyScrollLock } from "@/hooks/useBodyScrollLock";
import { useSelectedDetailEnrichment } from "@/hooks/useSelectedDetailEnrichment";
import { useSearchState } from "@/hooks/useSearchState";

export default function Home() {
  const {
    query, setQuery, active, view, setView,
    objectType, setObjectType, monumentAard, setMonumentAard,
    province, setProvince, municipality, setMunicipality,
    functionFilter, setFunctionFilter, matchSourceFilter, setMatchSourceFilter,
    excludedStatuses, toggleLegalStatus, onlyGroenaanleg, setOnlyGroenaanleg, onlyMsp, setOnlyMsp,
    selected, setSelected, choose, filters, setFilters,
    remoteState, hasMore, loadingMore,
    baseResults, functions, provinces, municipalities, matchSources, results,
    executeSearch, executeConceptSearch, browseType, loadMore, reset,
  } = useSearchState();
  const { suggestions, suggestionsOpen, setSuggestionsOpen, activeSuggestion, setActiveSuggestion, commitSuggestion, handleQueryKeyDown } = useTermSuggestions(query, active, setQuery);
  const { complexMembers, onderzoeksgebiedVerrijking } = useSelectedDetailEnrichment(selected);
  useBodyScrollLock(Boolean(selected));

  function submitSearch(event: FormEvent) {
    event.preventDefault();
    void executeSearch(query.trim());
  }

  return <main>
    <div className="govbar">
      {/* eslint-disable-next-line @next/next/no-img-element */}
      <img src="/logo-rce.gif" alt="Rijksdienst voor het Cultureel Erfgoed, Ministerie van Onderwijs, Cultuur en Wetenschap" />
    </div>
    <header>
      <button className="brand brand-button" type="button" onClick={reset} aria-label="Terug naar de startpagina"><b>D</b><span><strong>Doorzoeker</strong><small>Rijksmonumenten in actuele RCE-data</small></span></button>
      <p><i />Live gekoppeld aan RCE Linked Data <a href="https://linkeddata.cultureelerfgoed.nl/rce/cho" target="_blank" rel="noreferrer">Bronverantwoording</a></p>
    </header>
    <section className="hero">
      <small>OFFICIËLE RIJKSMONUMENTENDATA VAN DE RCE</small><h1>Zoek een rijksmonument en bekijk de officiële registratie.</h1>
      <p className="hero-intro">Zoek op monumentnummer, plaats, oorspronkelijke functie, monumentaard of woorden uit de formele omschrijving.</p>
      <div className="search-combobox"><form onSubmit={(event) => { setSuggestionsOpen(false); submitSearch(event); }}><span aria-hidden="true">⌕</span><label className="sr" htmlFor="q">Zoeken</label><input id="q" role="combobox" aria-autocomplete="list" aria-expanded={suggestionsOpen} aria-controls="term-suggestions" aria-activedescendant={activeSuggestion >= 0 ? `term-suggestion-${activeSuggestion}` : undefined} value={query} onFocus={() => setSuggestionsOpen(suggestions.length > 0)} onBlur={() => setSuggestionsOpen(false)} onChange={(event) => setQuery(event.target.value)} onKeyDown={handleQueryKeyDown} placeholder="Bijvoorbeeld 36046, woonhuis, lijstgevel of Utrecht" />{query && <button type="button" className="clear" onClick={() => { setQuery(""); setSuggestionsOpen(false); }} aria-label="Zoekveld wissen">×</button>}<button type="submit">Doorzoek RCE</button></form>{suggestionsOpen && <ul id="term-suggestions" className="term-suggestions" role="listbox" aria-label="Termsuggesties">{suggestions.map((suggestion, index) => <li key={suggestion.uri} id={`term-suggestion-${index}`} role="option" aria-selected={index === activeSuggestion} className={index === activeSuggestion ? "active" : ""}><button type="button" onMouseDown={(event) => event.preventDefault()} onMouseEnter={() => setActiveSuggestion(index)} onClick={() => commitSuggestion(suggestion)}>{suggestion.label}<small>{suggestion.sourceName}</small></button></li>)}</ul>}</div>
      <nav aria-label="Direct zoeken">Direct zoeken: {["36046", "Woonhuis", "Archeologisch", "Utrecht"].map((term) => <button type="button" key={term} onClick={() => void executeSearch(term)}>{term}</button>)}</nav>
      <nav aria-label="Bekijk een volledige collectie">Bekijk alles: <button type="button" onClick={() => void browseType("werelderfgoed")}>Werelderfgoed</button><button type="button" onClick={() => void browseType("gezicht")}>Gezichten</button><button type="button" onClick={() => void browseType("complex")}>Complexen</button></nav>
      <p className={`source-status ${remoteState}`} aria-live="polite">{remoteState === "loading" ? "De actuele RCE-registratie wordt doorzocht…" : remoteState === "error" ? "De RCE Linked Data-service is momenteel niet bereikbaar. Probeer het later opnieuw." : remoteState === "success" ? "Resultaten rechtstreeks uit de actuele RCE Linked Data" : "Actuele brondata · formele registraties · juridische status rijksmonument"}</p>
    </section>
    <section className={`work ${!active ? "start" : ""}`}>
      <aside className={filters ? "show" : ""} aria-label="Zoekfilters">
        <div className="aside-title"><div><small>VERFIJN</small><h2>Filters</h2></div><button type="button" onClick={() => setFilters(false)} aria-label="Filters sluiten">×</button></div>
        <fieldset><legend>Soort object</legend><details className="hint"><summary>Wat betekent dit?</summary><p>Rijksmonument, Werelderfgoed, Gezicht, Complex en Onderzoeksgebied zijn vijf losse soorten object, geen varianten van hetzelfde. Werelderfgoed en Gezicht zijn gebieden waar de RCE verantwoordelijk voor is en die rijksmonumenten kunnen bevatten. Een Complex is zelf geen monument, maar een samenhang tussen meerdere rijksmonumenten. Een Onderzoeksgebied staat los van het monumentenregister.</p></details>{["Alle", "Rijksmonument", "Werelderfgoed", "Gezicht", "Complex", "Onderzoeksgebied"].map((option) => <label key={option}><input type="radio" name="soort" checked={objectType === option} onChange={() => setObjectType(option)} /><span>{option === "Alle" ? "Alle soorten" : option}</span><em>{option === "Alle" ? baseResults.length : baseResults.filter((item) => item.objectType === option).length}</em></label>)}</fieldset>
        <fieldset><legend>Monumentaard</legend><details className="hint"><summary>Wat betekent dit?</summary><p>Gebouwd of archeologisch geldt alleen voor Rijksmonumenten. De andere soorten object hebben geen monumentaard.</p></details>{["Alle", "Gebouwd", "Archeologisch"].map((option) => <label key={option}><input type="radio" name="aard" checked={monumentAard === option} onChange={() => setMonumentAard(option)} /><span>{option === "Alle" ? "Alle monumentaarden" : option}</span><em>{option === "Alle" ? baseResults.filter((item) => item.objectType === "Rijksmonument").length : baseResults.filter((item) => item.monumentAard === option).length}</em></label>)}</fieldset>
        {provinces.length > 0 && <fieldset><legend>Provincie</legend><label className="select-label"><span className="sr">Filter op provincie</span><select aria-label="Filter op provincie" value={province} onChange={(event) => { setProvince(event.target.value); setMunicipality("Alle"); }}><option value="Alle">Alle provincies ({baseResults.length})</option>{provinces.map((option) => <option key={option} value={option}>{option} ({baseResults.filter((item) => item.province === option).length})</option>)}</select></label></fieldset>}
        {municipalities.length > 0 && <fieldset><legend>Gemeente / woonplaats</legend><label className="select-label"><span className="sr">Filter op gemeente of woonplaats</span><select aria-label="Filter op gemeente of woonplaats" value={municipality} onChange={(event) => setMunicipality(event.target.value)}><option value="Alle">Alle plaatsen ({baseResults.filter((item) => province === "Alle" || item.province === province).length})</option>{municipalities.map((option) => <option key={option} value={option}>{option} ({baseResults.filter((item) => item.municipality === option && (province === "Alle" || item.province === province)).length})</option>)}</select></label></fieldset>}
        <fieldset><legend>Functie</legend><label className="select-label"><span className="sr">Filter op functie</span><select aria-label="Filter op functie" value={functionFilter} onChange={(event) => setFunctionFilter(event.target.value)}><option value="Alle">Alle functies ({baseResults.length})</option>{functions.map((option) => <option key={option} value={option}>{option} ({baseResults.filter((item) => [item.kind, ...(item.originalFunctionNames ?? []), ...(item.currentFunctionNames ?? [])].includes(option)).length})</option>)}</select></label></fieldset>
        {matchSources.length > 0 && <fieldset><legend>Gevonden via</legend><details className="hint"><summary>Wat betekent dit?</summary><p>Matchbron laat zien via welk gegevensveld je zoekterm is gevonden, bijvoorbeeld de functie, de omschrijving of de plaats. Dat is niet per se de naam van het monument.</p></details><label className="select-label"><span className="sr">Filter op matchbron</span><select aria-label="Filter op matchbron" value={matchSourceFilter} onChange={(event) => setMatchSourceFilter(event.target.value)}><option value="Alle">Alle matchbronnen</option>{matchSources.map((option) => <option key={option} value={option}>{option} ({baseResults.filter((item) => item.matchSource === option).length})</option>)}</select></label></fieldset>}
        <fieldset><legend>Juridische status</legend><details className="hint"><summary>Wat betekent dit?</summary><p>De status verschilt per soort object. Een Rijksmonument heeft de status &ldquo;rijksmonument&rdquo;, Werelderfgoed en Gezicht hebben hun eigen status, en een Complex of Onderzoeksgebied is zelf geen aangewezen monument.</p></details>{[...new Set(baseResults.map((item) => statusLabel(item.objectType)))].map((label) => <label key={label}><input type="checkbox" checked={!excludedStatuses.includes(label)} onChange={() => toggleLegalStatus(label)} /><span>{label}</span><em>{baseResults.filter((item) => statusLabel(item.objectType) === label).length}</em></label>)}</fieldset>
        {(baseResults.some((item) => item.groenaanleg) || baseResults.some((item) => item.msp)) && <fieldset><legend>Kenmerken</legend><details className="hint"><summary>Wat betekent dit?</summary><p>Historische aanleg (groenaanleg) betekent dat er een tuin- of parkaanleg bij het monument hoort. Monumenten Selectie Project verwijst naar een aanwijzingsronde van de RCE tussen ongeveer 1997 en 2002, gericht op gebouwen uit 1850-1940.</p></details>
          {baseResults.some((item) => item.groenaanleg) && <label><input type="checkbox" checked={onlyGroenaanleg} onChange={(event) => setOnlyGroenaanleg(event.target.checked)} /><span>Historische aanleg (groenaanleg)</span><em>{baseResults.filter((item) => item.groenaanleg).length}</em></label>}
          {baseResults.some((item) => item.msp) && <label><input type="checkbox" checked={onlyMsp} onChange={(event) => setOnlyMsp(event.target.checked)} /><span>Monumenten Selectie Project</span><em>{baseResults.filter((item) => item.msp).length}</em></label>}
        </fieldset>}
        <button className="reset" type="button" onClick={reset}>Wis alle filters</button>
      </aside>
      <div className="results">
        <div className="toolbar"><div><small>RIJKSMONUMENTEN</small><h2 aria-live="polite">{results.length} {results.length === 1 ? "resultaat" : "resultaten"}{active ? ` voor “${active}”` : ""}</h2></div><div><button className="mobile-filter" type="button" onClick={() => setFilters(true)}>☰ Filters</button><span className="switch" aria-label="Weergave"><button type="button" className={view === "list" ? "on" : ""} onClick={() => setView("list")} aria-label="Lijstweergave" aria-pressed={view === "list"}>☷</button><button type="button" className={view === "map" ? "on" : ""} onClick={() => setView("map")} aria-label="Kaartweergave" aria-pressed={view === "map"}>⌖</button></span></div></div>
        {remoteState === "idle" ? <div className="start-panel"><small>ZO WERKT HET</small><h2>Wat deze zoekmachine doet</h2><p>Doorzoeker doorzoekt de actuele CHO-dataset van de Rijksdienst voor het Cultureel Erfgoed en laat bij elk resultaat het gegevensveld zien waarin de zoekterm is gevonden.</p><div><article><b>01</b><h3>Zoek breed</h3><p>Gebruik een nummer, plaats, functie, monumentaard of omschrijving.</p></article><article><b>02</b><h3>Matchbron per resultaat</h3><p>Elk resultaat vermeldt de matchbron en de geregistreerde waarde.</p></article><article><b>03</b><h3>Controleer de bron</h3><p>Bekijk functie, adres, geometrie, percelen en de canonieke RCE-link.</p></article></div></div> : remoteState === "loading" ? <div className="empty"><b>…</b><h3>RCE Linked Data doorzoeken</h3><p>Een ogenblik; de officiële bron wordt geraadpleegd.</p></div> : results.length === 0 ? <div className="empty"><b>0</b><h3>Geen monumenten gevonden</h3><p>Probeer een plaats, postcode, formele functie, monumentaard of monumentnummer.</p><button type="button" onClick={reset}>Nieuwe zoekopdracht</button></div> : view === "list" ? <div className="cards">{results.map((item) => { const badge = typeBadge(item); return <article key={item.id}><div className={`tile ${badge.modifier}${item.image ? " has-image" : ""}`.trim()} style={item.image ? { backgroundImage: `url(${item.image.url})` } : undefined}>{item.image ? <span className="tile-badge">{badge.letter}</span> : <><b>{badge.letter}</b><small>RCE register</small></>}</div><div className="copy"><small>{item.kind}<code>RM {item.monumentNumber ?? item.id}</code></small><h3>{item.title}</h3><p className="address">● {item.address}{item.postalCode || item.place ? `, ${item.postalCode} ${item.place}` : ""}</p><p>{item.description}</p><span>{item.objectType === "Rijksmonument" ? (item.monumentAardConcept ? <button type="button" className="concept-link" onClick={() => void executeConceptSearch(item.monumentAardConcept!)} title="Zoek alle rijksmonumenten met deze monumentaard">{item.monumentAard ?? "Rijksmonument"}</button> : (item.monumentAard ?? "Rijksmonument")) : item.objectType}</span><span>{item.period}</span></div><button className="open" type="button" onClick={() => setSelected(item)} aria-label={`Details van ${item.title}`}>→</button></article>; })}</div> : <div className="map-view"><HeritageMap items={results.filter((item) => item.lat && item.lng)} onSelect={(mapItem) => { const item = results.find((candidate) => candidate.id === mapItem.id); if (item) choose(item); }} /><div className="map-object-list"><h3>Objecten op deze kaart</h3><ul>{results.filter((item) => item.lat && item.lng).map((item) => <li key={item.id}><button type="button" onClick={() => choose(item)}>{item.title}</button></li>)}</ul></div></div>}
        {hasMore && remoteState === "success" && view === "list" && <div className="more-results"><button type="button" onClick={() => void loadMore()} disabled={loadingMore}>{loadingMore ? "Meer RCE-resultaten laden…" : "Laad 25 volgende resultaten"}</button><small>{baseResults.length} unieke monumenten geladen</small></div>}
      </div>
    </section>
    {selected && <div className="backdrop" role="presentation" onMouseDown={(event) => { if (event.target === event.currentTarget) setSelected(null); }}><aside className="detail" role="dialog" aria-modal="true" aria-labelledby="detail-title"><button className="x" type="button" onClick={() => setSelected(null)} aria-label="Details sluiten">×</button><div className={`detail-head ${typeBadge(selected).modifier}${selected.image ? " has-image" : ""}`.trim()} style={selected.image ? { backgroundImage: `linear-gradient(0deg, #00000073, #00000073), url(${selected.image.url})` } : undefined}>{selected.image ? <span className="tile-badge large">{typeBadge(selected).letter}</span> : <b>{typeBadge(selected).letter}</b>}<small>{selected.official ? "RCE Linked Data" : `${selected.objectType} erfgoed`}</small></div><div className="detail-copy"><small>{selected.objectType === "Werelderfgoed" ? "WERELDERFGOED" : selected.objectType === "Gezicht" ? "GEZICHT" : selected.objectType === "Complex" ? "COMPLEX" : selected.objectType === "Onderzoeksgebied" ? "ONDERZOEKSGEBIED" : "RIJKSMONUMENT"} {selected.monumentNumber ?? selected.id}</small><h2 id="detail-title">{selected.title}</h2><p>{selected.address}<br />{selected.postalCode} {selected.place}{selected.province ? `, ${selected.province}` : ""}</p><hr /><p>{selected.description}</p>{selected.objectType === "Complex" && complexMembers && complexMembers.complexUri === selected.linkedDataUrl && complexMembers.members.some((member) => member.lat != null && member.lng != null) ? <div className="detail-map"><HeritageMap items={complexMembers.members.flatMap((member) => member.lat != null && member.lng != null ? [{ id: member.choUri, title: member.name, address: "", place: "", objectType: "Complex" as const, lat: member.lat, lng: member.lng, wkt: member.wkt, forceArea: true }] : [])} onSelect={() => {}} compact /></div> : selected.lat && selected.lng ? <div className="detail-map"><HeritageMap items={[selected]} onSelect={() => {}} compact /></div> : null}<dl><div><dt>Status</dt><dd>{statusLabel(selected.objectType)}</dd></div><div><dt>CHO-nummer</dt><dd>{selected.objectNumber} <details className="hint-inline"><summary>Wat is dit?</summary><p>Het CHO-nummer is RCE&apos;s eigen nummer voor elk record in de Linked Data, los van het rijksmonument-, complex- of werelderfgoednummer.</p></details></dd></div><div><dt>Functie</dt><dd>{selected.kind}</dd></div>{selected.monumentAardConcept ? <div><dt>Monumentaard</dt><dd><button type="button" className="concept-link" onClick={() => void executeConceptSearch(selected.monumentAardConcept!)} title="Zoek alle rijksmonumenten met deze monumentaard">{selected.monumentAardConcept.label}</button></dd></div> : null}<div><dt>Registratie / datering</dt><dd>{selected.registrationDate ?? selected.period}</dd></div>{selected.wkt && <div><dt>Geometrie</dt><dd><details><summary>Toon ruwe WKT (WGS84)</summary><code>{selected.wkt}</code></details></dd></div>}{selected.parcels?.length ? <div><dt>Kadastrale percelen</dt><dd>{selected.parcels.map((parcel) => <span key={`${parcel.municipalityCode}-${parcel.section}-${parcel.parcelNumber}`}>{parcel.municipality} {parcel.section} {parcel.parcelNumber}{parcel.provinceCode ? ` (${parcel.provinceCode})` : ""}</span>)}</dd></div> : null}{selected.archaeologicalSites?.length ? <div><dt>Archeologisch terrein</dt><dd>{selected.archaeologicalSites.map((site, index) => <span key={site.archisMonumentnummer ?? index}>{site.archisMonumentnummer ? `Archis-monumentnummer ${site.archisMonumentnummer}` : "Archis-monumentnummer onbekend"}{site.waardering ? <> — {site.waarderingConceptUri ? <button type="button" className="concept-link" onClick={() => void executeConceptSearch({ uri: site.waarderingConceptUri!, label: site.waardering! }, "waardering")} title="Zoek alle rijksmonumenten met deze archeologische waardering">{site.waardering}</button> : site.waardering}</> : ""}</span>)}</dd></div> : null}{selected.complexes?.length ? <div><dt>Onderdeel van complex</dt><dd>{selected.complexes.map((complex, index) => <span key={complex.complexnummer ?? index}>{complex.complexnaam || (complex.complexnummer ? `Complex ${complex.complexnummer}` : "Complex")}{complex.complexnummer && complex.complexnaam ? ` (${complex.complexnummer})` : ""}{complex.role === "hoofdobject" ? " — hoofdobject" : ""}</span>)}</dd></div> : null}{selected.objectType === "Complex" && selected.complexMemberCount ? <div><dt>Aantal onderdelen</dt><dd>{selected.complexMemberCount} rijksmonument{selected.complexMemberCount === 1 ? "" : "en"}</dd></div> : null}{selected.groenaanleg && (selected.groenaanleg.typeAanleg || selected.groenaanleg.categorie) ? <div><dt>Historische aanleg</dt><dd>{[selected.groenaanleg.typeAanleg, selected.groenaanleg.categorie].filter(Boolean).join(" — ")}</dd></div> : null}{selected.msp ? <div><dt>Monumenten Selectie Project</dt><dd>Aangewezen via het Monumenten Selectie Project (circa 1997-2002)</dd></div> : null}<div><dt>Bron</dt><dd>{selected.official ? "RCE Linked Data" : "Voorbeelddata"}</dd></div></dl>{selected.image ? <p className="detail-image-credit"><small>Foto{selected.image.title ? `: ${selected.image.title}` : ""} — RCE Beeldbank{selected.image.sourceUrl ? <> (<a href={selected.image.sourceUrl} target="_blank" rel="noreferrer">bron</a>)</> : ""}{selected.image.license ? <> · <a href={selected.image.license} target="_blank" rel="noreferrer">licentie</a></> : ""}</small></p> : null}{selected.objectType === "Complex" && complexMembers && complexMembers.complexUri === selected.linkedDataUrl && complexMembers.members.length ? <div className="map-object-list"><h3>Onderdelen van dit complex</h3><ul>{complexMembers.members.map((member) => <li key={member.choUri}><button type="button" onClick={() => void executeSearch(member.monumentNumber)}>{member.name}{member.isHoofdobject ? " — hoofdobject" : ""}</button></li>)}</ul></div> : null}{selected.objectType === "Onderzoeksgebied" && onderzoeksgebiedVerrijking && onderzoeksgebiedVerrijking.gebiedUri === selected.linkedDataUrl ? <div className="map-object-list"><h3>Archeologisch onderzoek binnen dit gebied</h3>{onderzoeksgebiedVerrijking.complexen.length ? <ul>{onderzoeksgebiedVerrijking.complexen.map((complex) => <li key={complex.complexUri}><a href={complex.complexUri} target="_blank" rel="noreferrer">{complex.typeLabel || `Archeologisch complex ${complex.choNumber}`}</a></li>)}</ul> : null}{onderzoeksgebiedVerrijking.vondstlocaties.length ? <ul>{onderzoeksgebiedVerrijking.vondstlocaties.map((vl) => <li key={vl.vlUri}><a href={vl.vlUri} target="_blank" rel="noreferrer">{vl.locatienaam || `Vondstlocatie ${vl.choNumber}`}</a></li>)}</ul> : null}<p>{onderzoeksgebiedVerrijking.vondstlocatieTotaal ? `${onderzoeksgebiedVerrijking.vondstlocatieTotaal} vondstlocatie${onderzoeksgebiedVerrijking.vondstlocatieTotaal === 1 ? "" : "s"}${onderzoeksgebiedVerrijking.vondstlocatieTotaal > onderzoeksgebiedVerrijking.vondstlocaties.length ? ` (eerste ${onderzoeksgebiedVerrijking.vondstlocaties.length} getoond)` : ""}, ${onderzoeksgebiedVerrijking.grondsporenTotaal} grondspoor/grondsporen, ${onderzoeksgebiedVerrijking.vondstenTotaal} vondst(en)${onderzoeksgebiedVerrijking.complexenViaVondstlocatieTotaal ? ` en ${onderzoeksgebiedVerrijking.complexenViaVondstlocatieTotaal} archeologisch complex(en)` : ""} binnen dit gebied.` : "Geen gekoppeld archeologisch onderzoek gevonden voor dit gebied."}</p></div> : null}{selected.literature?.length ? <div className="map-object-list"><h3>Literatuur</h3><ul>{selected.literature.map((ref) => <li key={ref.uri}>{ref.sourceUrl ? <a href={ref.sourceUrl} target="_blank" rel="noreferrer">{ref.title}</a> : <span>{ref.title}</span>}{ref.authors.length ? ` — ${ref.authors.join(", ")}` : ""}{ref.year ? ` (${ref.year})` : ""}</li>)}</ul></div> : null}<div className="detail-links"><a href={selected.sourceUrl ?? `${MONUMENT_REGISTER_BASE_URL}${encodeURIComponent(selected.monumentNumber ?? selected.id)}`} target="_blank" rel="noreferrer">{selected.objectType === "Werelderfgoed" ? "Bekijk op de UNESCO Werelderfgoedlijst" : selected.objectType === "Gezicht" ? "Bekijk in het Archis-archief" : selected.objectType === "Complex" || selected.objectType === "Onderzoeksgebied" ? "Bekijk in de RCE Linked Data" : "Bekijk in het Monumentenregister"} <b>→</b></a>{selected.linkedDataUrl && selected.linkedDataUrl !== selected.sourceUrl && <a href={selected.linkedDataUrl} target="_blank" rel="noreferrer">Bekijk in de RCE Linked Data <b>→</b></a>}</div><blockquote>{selected.official ? "Actueel record uit de officiële Linked Data Voorziening van de RCE." : "Voorbeeldrecord; nog niet alle zoekvelden zijn live gekoppeld."}</blockquote></div></aside></div>}
  </main>;
}
